garden:
    shell: zsh -c
    interactive-shell: zsh

commands:
    shell-words: |
        args='a b c'
        for arg in $args
        do
            echo $arg
        done

    echo-shell: |
        #!echo
        hello
        world
