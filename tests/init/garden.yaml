---
variables:
  gh_ssh: "git@github.com"
  repos: repos

trees:
  example/tree:
    environment:
      PATH: "${TREE_PATH}"
    path: example/tree/repo
    remotes:
      publish: "${gh_ssh}:user/example.git"
    # example/repo0git is created by the integration test
    url: ${repos}/example.git
